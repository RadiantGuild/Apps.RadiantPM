export interface Configuration {
    gzip?: boolean;
    favicon: {
        light: string;
        dark: string;
        default: string;
    };
    logo: {
        light: string;
        dark: string;
    };
    logoText: {
        light: string;
        dark: string;
    };
}
